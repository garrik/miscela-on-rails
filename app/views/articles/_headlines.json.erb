[
  <% @articles.each_with_index do |article, index| %>
    { "id": "<%= article.id %>", "url": "<%= article_path(article.id) %>", "title": "<%= article.title %>", "content": "<%= article.content %>" }
    <% unless index == @articles.length - 1 %>,<% end %>
  <% end %>
]